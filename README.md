# fileserver
Just a fileserver

## Build
```
make
```

## Usage
```
Usage of bin/fileserver:
  -dir string
    	path where the files to serve will be fetched (default ".")
  -port string
    	server listening port (default "8080")
```
